const cfg=window.BOOKS_GATE_CONFIG||{};const configured=cfg.SUPABASE_URL&&!cfg.SUPABASE_URL.includes("COLLE_ICI")&&cfg.SUPABASE_ANON_KEY&&!cfg.SUPABASE_ANON_KEY.includes("COLLE_ICI");let supabaseClient=null;if(configured){supabaseClient=window.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY)}function isNew(d){if(!d)return false;return (new Date()-new Date(d))/86400000<=7}function qs(n){return new URLSearchParams(location.search).get(n)}function esc(v=""){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]))}function notice(el){if(!configured){el.innerHTML='<div class="notice">Le site est prêt. Il reste à renseigner <b>config.js</b> avec ton URL Supabase et ta clé publique.</div>';return true}return false}